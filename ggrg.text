<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Victor Jack | Software Engineer</title>
  <meta name="description" content="Victor Jack is a Software Engineer specialized in building exceptional digital experiences" />
  
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg-dark: #0a0a0a;
      --bg-card: #111111;
      --text-primary: #ffffff;
      --text-secondary: #888888;
      --accent-green: #00ff88;
      --accent-dim: #00cc6a;
      --border: #222222;
    }

    body.light-mode {
      --bg-dark: #ffffff;
      --bg-card: #f5f5f5;
      --text-primary: #0a0a0a;
      --text-secondary: #666666;
      --accent-green: #00cc6a;
      --accent-dim: #00ff88;
      --border: #e0e0e0;
    }

    body {
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: var(--bg-dark);
      color: var(--text-primary);
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* Navigation */
    nav {
      position: fixed;
      top: 0;
      width: 100%;
      background: rgba(10, 10, 10, 0.7);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      z-index: 1000;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .nav-inner {
      max-width: 1400px;
      margin: 0 auto;
      padding: 1.5rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .nav-logo {
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--accent-green);
    }

    .nav-links {
      display: flex;
      gap: 2rem;
    }

    .nav-links button {
      background: none;
      border: none;
      color: var(--text-secondary);
      cursor: pointer;
      font-size: 0.95rem;
      font-weight: 500;
      transition: color 0.3s;
      font-family: inherit;
    }

    .nav-links button:hover,
    .nav-links button.active {
      color: var(--text-primary);
    }

    .menu-toggle {
      display: none;
      background: none;
      border: none;
      color: var(--text-primary);
      font-size: 1.5rem;
      cursor: pointer;
      padding: 0.5rem;
    }

    .theme-toggle {
      background: none;
      border: 2px solid var(--border);
      color: var(--text-primary);
      font-size: 1rem;
      cursor: pointer;
      padding: 0.4rem 0.6rem;
      border-radius: 8px;
      transition: all 0.3s;
      margin-right: 0.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .theme-toggle:hover {
      border-color: var(--accent-green);
      color: var(--accent-green);
    }

    /* Hero Section */
    #hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8rem 2rem 4rem;
      position: relative;
    }

    .hero-content {
      max-width: 1000px;
      text-align: center;
    }

    .retro-subtitle {
      color: var(--accent-green);
      font-size: 0.9rem;
      font-weight: 600;
      letter-spacing: 2px;
      margin-bottom: 1.5rem;
      animation: fadeIn 0.8s ease-out;
    }

    .retro-title {
      font-size: clamp(2.5rem, 8vw, 5rem);
      font-weight: 700;
      margin-bottom: 1rem;
      line-height: 1.2;
      animation: fadeIn 0.8s ease-out 0.2s backwards;
    }

    .hero-content > p {
      font-size: 1.15rem;
      color: var(--text-secondary);
      max-width: 700px;
      margin: 2rem auto;
      line-height: 1.8;
      animation: fadeIn 0.8s ease-out 0.4s backwards;
    }

    .hero-content strong {
      color: var(--accent-green);
      font-weight: 600;
    }

    .hero-buttons {
      display: flex;
      gap: 1.5rem;
      justify-content: center;
      margin: 3rem 0;
      animation: fadeIn 0.8s ease-out 0.6s backwards;
      flex-wrap: wrap;
    }

    .btn {
      padding: 1rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
      transition: all 0.3s;
      display: inline-block;
    }

    .btn-primary {
      background: var(--accent-green);
      color: var(--bg-dark);
    }

    .btn-primary:hover {
      background: var(--accent-dim);
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(0, 255, 136, 0.2);
    }

    .btn-outline {
      border: 2px solid var(--border);
      color: var(--text-primary);
    }

    .btn-outline:hover {
      border-color: var(--accent-green);
      color: var(--accent-green);
      transform: translateY(-2px);
    }

    .social-links {
      display: flex;
      justify-content: center;
      gap: 2rem;
      font-size: 1rem;
      animation: fadeIn 0.8s ease-out 0.8s backwards;
    }

    .social-links a {
      color: var(--text-secondary);
      text-decoration: none;
      transition: color 0.3s;
    }

    .social-links a:hover {
      color: var(--accent-green);
    }

    /* Section Styling */
    section {
      padding: 6rem 2rem;
      max-width: 1400px;
      margin: 0 auto;
      scroll-margin-top: 80px;
    }

    .section-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-align: center;
      padding-top: 2rem;
    }

    .section-subtitle {
      text-align: center;
      color: var(--text-secondary);
      font-size: 1.15rem;
      margin-bottom: 4rem;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
    }

    /* Skills Grid */
    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      margin: 4rem 0;
    }

    .skill-card {
      background: var(--bg-card);
      padding: 2rem;
      border-radius: 12px;
      border: 1px solid var(--border);
      transition: all 0.3s;
    }

    .skill-card:hover {
      border-color: var(--accent-green);
      transform: translateY(-5px);
    }

    .skill-card strong {
      color: var(--accent-green);
      font-size: 1.1rem;
      display: block;
      margin-bottom: 0.5rem;
    }

    .skill-card p {
      color: var(--text-secondary);
      font-size: 0.95rem;
    }

    /* Status Box */
    .status-box {
      background: var(--bg-card);
      border: 1px solid var(--accent-green);
      border-radius: 12px;
      padding: 2rem;
      margin: 4rem auto;
      max-width: 600px;
      text-align: center;
    }

    .status-box .status-text {
      color: var(--accent-green);
      font-weight: 600;
      font-size: 1.1rem;
    }

    /* Timeline */
    .timeline {
      max-width: 900px;
      margin: 0 auto;
      position: relative;
    }

    .timeline::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 2px;
      background: var(--border);
    }

    .timeline-entry {
      position: relative;
      padding-left: 3rem;
      padding-bottom: 3rem;
    }

    .timeline-entry::before {
      content: '';
      position: absolute;
      left: -6px;
      top: 0;
      width: 14px;
      height: 14px;
      border-radius: 50%;
      background: var(--accent-green);
      border: 3px solid var(--bg-dark);
    }

    .timeline-entry h3 {
      font-size: 1.3rem;
      margin-bottom: 0.5rem;
      color: var(--text-primary);
    }

    .timeline-entry .date {
      color: var(--accent-green);
      font-size: 0.9rem;
      margin-bottom: 1rem;
      display: block;
    }

    .timeline-entry p {
      color: var(--text-secondary);
      margin-bottom: 0.75rem;
    }

    .timeline-entry small {
      color: var(--text-secondary);
      font-size: 0.85rem;
    }

    /* Projects Grid */
    .projects-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }

    .project-card {
      background: var(--bg-card);
      border-radius: 12px;
      overflow: hidden;
      border: 1px solid var(--border);
      transition: all 0.3s;
    }

    .project-card:hover {
      border-color: var(--accent-green);
      transform: translateY(-5px);
    }

    .project-image {
      height: 200px;
      background: var(--border);
      position: relative;
      overflow: hidden;
    }

    .project-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .project-content {
      padding: 1.5rem;
    }

    .project-content h3 {
      font-size: 1.3rem;
      margin-bottom: 0.75rem;
    }

    .project-content p {
      color: var(--text-secondary);
      margin-bottom: 1rem;
      font-size: 0.95rem;
    }

    .project-tech {
      color: var(--accent-green);
      font-size: 0.85rem;
      margin-bottom: 1rem;
    }

    .project-links a {
      color: var(--accent-green);
      text-decoration: none;
      margin-right: 1rem;
      font-size: 0.9rem;
      transition: color 0.3s;
    }

    .project-links a:hover {
      color: var(--accent-dim);
    }

    /* Contact Section */
    #contact {
      text-align: center;
    }

    .contact-info {
      margin: 3rem 0;
    }

    .contact-info a {
      color: var(--accent-green);
      text-decoration: none;
      font-size: 1.3rem;
      display: block;
      margin: 1rem 0;
      transition: color 0.3s;
    }

    .contact-info a:hover {
      color: var(--accent-dim);
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 3rem 2rem;
      border-top: 1px solid var(--border);
      color: var(--text-secondary);
      font-size: 0.9rem;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive */
    @media (max-width: 768px) {
      .nav-inner {
        padding: 1rem 1.5rem;
      }

      .menu-toggle {
        display: block;
      }

      .theme-toggle {
        margin-right: 0;
      }

      .nav-links {
        position: fixed;
        top: 70px;
        left: 0;
        right: 0;
        background: rgba(10, 10, 10, 0.98);
        backdrop-filter: blur(10px);
        flex-direction: column;
        padding: 2rem;
        gap: 1.5rem;
        border-bottom: 1px solid var(--border);
        transform: translateY(-100%);
        opacity: 0;
        transition: all 0.3s;
        pointer-events: none;
      }

      .nav-links.active {
        transform: translateY(0);
        opacity: 1;
        pointer-events: all;
      }

      .nav-links button {
        font-size: 1.1rem;
        text-align: left;
        width: 100%;
        padding: 0.5rem 0;
      }

      section {
        padding: 4rem 1.5rem;
      }

      .skills-grid,
      .projects-grid {
        grid-template-columns: 1fr;
      }

      .hero-buttons {
        flex-direction: column;
        align-items: center;
      }

      .btn {
        width: 100%;
        max-width: 300px;
      }

      .social-links {
        flex-direction: column;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>

  <!-- Navigation -->
  <nav>
    <div class="nav-inner">
      <div class="nav-logo">Victor Jack</div>
      <div style="display: flex; align-items: center; gap: 0.5rem;">
        <button class="theme-toggle" id="themeToggle">🌙</button>
        <button class="menu-toggle" onclick="toggleMenu()">☰</button>
        <div class="nav-links" id="navLinks">
          <button class="active" onclick="scrollToSection('hero')">Home</button>
          <button onclick="scrollToSection('about')">About</button>
          <button onclick="scrollToSection('experience')">Experience</button>
          <button onclick="scrollToSection('projects')">Projects</button>
          <button onclick="scrollToSection('contact')">Contact</button>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <section id="hero">
    <div class="hero-content">
      <p class="retro-subtitle">&gt; FULL STACK DEVELOPER</p>
      <h1 class="retro-title">Victor Jack</h1>
      <p>
        Hi, my name is <strong>Victor Jack</strong>, I am a software Engineer, a true definition of a Philomath and a Tech addict. I strive to bring innovative ideas to life. My goal is to craft seamless digital experiences that not only meet but exceed client expectations.
      </p>
      <div class="hero-buttons">
        <a href="#projects" class="btn btn-primary">VIEW PROJECTS</a>
        <a href="/VictorJack.pdf" download class="btn btn-outline">DOWNLOAD CV</a>
      </div>
      <div class="social-links">
        <a href="https://github.com/thedon-dev" target="_blank">GitHub</a>
        <a href="https://linkedin.com/in/victorjackadokiye" target="_blank">LinkedIn</a>
        <a href="mailto:viktoh7351@gmail.com">Email</a>
      </div>
    </div>
  </section>

  <!-- About -->
  <section id="about">
    <h2 class="section-title">About Me</h2>
    <p class="section-subtitle">
      I'm a passionate software developer with a love for creating digital experiences that matter. My journey in tech started with curiosity and has evolved into a commitment to building softwares and solutions that bridge creativity and functionality.
    </p>
    
    <div class="skills-grid">
      <div class="skill-card">
        <strong>Frontend Development</strong>
        <p>React, TypeScript, Next.js, Tailwind CSS</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Building responsive and interactive user interfaces</p>
      </div>
      <div class="skill-card">
        <strong>Backend Development</strong>
        <p>Node.js, Express.js, MongoDB</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Scalable server-side applications and APIs</p>
      </div>
      <div class="skill-card">
        <strong>Mobile Development</strong>
        <p>React Native, Expo</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Building cross-platform mobile experiences with native performance</p>
      </div>
      <div class="skill-card">
        <strong>DevOps & Deployment</strong>
        <p>Docker, Vercel, CI/CD, Github, Pxxl</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Automated deployment and cloud infrastructure</p>
      </div>
    </div>
    
    <div class="status-box">
      <p class="status-text">&gt; CURRENT STATUS: AVAILABLE FOR PROJECTS</p>
      <p style="color: var(--text-secondary); margin-top: 0.5rem;">Ready to collaborate on your next big idea</p>
    </div>
  </section>

  <!-- Experience -->
  <section id="experience">
    <h2 class="section-title">Experience</h2>
    <p class="section-subtitle">
      My professional journey through the tech industry, working with innovative companies and delivering impactful solutions.
    </p>
    
    <div class="timeline">
      <div class="timeline-entry">
        <h3>Software Engineer @ Silicon Delta Innovation Hub</h3>
        <span class="date">Jul 2024 – Jul 2025 • Port-Harcourt</span>
        <p>Developed responsive user interfaces for clients and in-house projects. Collaborated with UX designers to implement pixel-perfect designs.</p>
        <small>React • Next.js • Tailwind • React Native</small>
      </div>
      
      <div class="timeline-entry">
        <h3>Frontend Developer @ Let's Build Labs</h3>
        <span class="date">Feb 2025 – Present • Remote</span>
        <p>Led development of scalable web3 applications using NextJS. Implemented CI/CD pipelines reducing deployment time by 40%.</p>
        <small>Next.js • TypeScript • Firebase • Web3</small>
      </div>
      
      <div class="timeline-entry">
        <h3>Frontend Developer @ Kleeth Technologies</h3>
        <span class="date">Duration • Location</span>
        <p>Led frontend development for cross-platform mobile and web applications, ensuring high-quality user experiences. Oversaw the entire deployment process, implementing and managing CI/CD pipelines to automate and accelerate application delivery.</p>
        <small>React • React Native • CI/CD</small>
      </div>
      
      <div class="timeline-entry">
        <h3>Frontend Developer @ Baanza App</h3>
        <span class="date">Duration • Location</span>
        <p>Built and maintained the user interface for both web and mobile applications. Developed responsive, high-performance frontends using ReactJS, Next.js, and React Native. Handled deployment and release processes for all applications.</p>
        <small>React • Next.js • React Native • CI/CD</small>
      </div>
    </div>
    
    <div class="status-box" style="margin-top: 4rem;">
      <p class="status-text">&gt; TOTAL EXPERIENCE: 3 YEARS+</p>
      <p style="color: var(--text-secondary); margin-top: 0.5rem;">Continuously learning and evolving with technology</p>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects">
    <h2 class="section-title">Projects</h2>
    <p class="section-subtitle">
      A collection of projects showcasing different aspects of modern web development, from user interfaces to backend architecture.
    </p>
    
    <div class="projects-grid">
      <div class="project-card">
        <div class="project-image">
          <img src="/Spa-Booking.png" alt="SPA Booking" onerror="this.parentElement.style.background='linear-gradient(135deg, #667eea 0%, #764ba2 100%)'">
        </div>
        <div class="project-content">
          <h3>SPA Booking System</h3>
          <p>Full-featured spa appointment system with customer booking interface and comprehensive admin dashboard for managing appointments and services.</p>
          <p class="project-tech">React • Node.js • MongoDB • Express</p>
          <div class="project-links">
            <a href="https://github.com/thedon-dev/salon_site" target="_blank">View Code →</a>
            <a href="https://salon-site-lilac.vercel.app" target="_blank">Live Demo →</a>
          </div>
        </div>
      </div>
      
      <!-- Add more project cards as needed -->
    </div>
  </section>

  <!-- Contact -->
  <section id="contact">
    <h2 class="section-title">Let's Work Together</h2>
    <p class="section-subtitle">
      Ready to collaborate on your next project? Let's connect and build something amazing together.
    </p>
    
    <div class="status-box">
      <p class="status-text">&gt; STATUS: AVAILABLE FOR WORK</p>
      <p style="color: var(--text-secondary); margin-top: 0.5rem;">Currently accepting new projects and collaborations. Response time: immediately</p>
    </div>
    
    <div class="contact-info">
      <a href="mailto:viktoh7351@gmail.com">viktoh7351@gmail.com</a>
      <a href="tel:+2349057696490">+234 905 7696 490</a>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>© 2025 Victor Jack — Built with passion and code</p>
  </footer>

  <script>
    // Theme toggle function
    function toggleTheme() {
      const body = document.body;
      const themeToggle = document.getElementById('themeToggle');
      
      body.classList.toggle('light-mode');
      
      if (body.classList.contains('light-mode')) {
        themeToggle.textContent = '☀️';
        localStorage.setItem('theme', 'light');
      } else {
        themeToggle.textContent = '🌙';
        localStorage.setItem('theme', 'dark');
      }
    }

    // Load saved theme on page load
    window.addEventListener('DOMContentLoaded', () => {
      const savedTheme = localStorage.getItem('theme');
      const themeToggle = document.getElementById('themeToggle');
      
      if (savedTheme === 'light') {
        document.body.classList.add('light-mode');
        themeToggle.textContent = '☀️';
      }

      // Add click listener to theme toggle
      themeToggle.addEventListener('click', toggleTheme);
    });

    function toggleMenu() {
      const navLinks = document.getElementById('navLinks');
      navLinks.classList.toggle('active');
    }

    function scrollToSection(id) {
      const element = document.getElementById(id);
      element.scrollIntoView({ behavior: 'smooth' });
      
      // Close mobile menu
      const navLinks = document.getElementById('navLinks');
      navLinks.classList.remove('active');
      
      // Update active state
      document.querySelectorAll('.nav-links button').forEach(btn => {
        btn.classList.remove('active');
      });
      event.target.classList.add('active');
    }

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  </script>

</body>
</html>